# 论文目录

- `sections/`：按章节拆分的正文。
- `figures/generated/`：已被论文引用的15张黑白最终图（PDF与300 dpi PNG）。
- `tables/generated/`：由脚本生成并被论文引用的最终表。

候选图表先保存在实验输出目录，确认进入论文后再通过脚本白名单晋级到这里。当前正文与附录合计44页，附录包含统计检验图、逐日流水、Markov持续性复核、偏离审计和真实建模代码摘录。
